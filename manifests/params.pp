class postfix::params {
    $ensure             = present
    $ensure_running     = true
    $ensure_enabled     = true
    $manage_instances   = false
    $config_source      = undef
    $config_template    = undef
    $instances          = undef
}
